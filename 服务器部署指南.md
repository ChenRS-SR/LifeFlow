# LifeFlow 服务器部署指南

> 适用版本：LifeFlow 2.0（`dev-2.0` 分支）  
> 部署方式：Docker Compose  
> 最后更新：2026-06-18

---

## 一、前置准备

### 1.1 服务器要求

| 配置项 | 最低要求 | 推荐 |
|---|---|---|
| CPU | 1 核 | 2 核 |
| 内存 | 2 GB | 4 GB |
| 磁盘 | 20 GB SSD | 40 GB SSD |
| 系统 | Ubuntu 20.04+ | Ubuntu 22.04 LTS |
| 网络 | 公网 IP | 公网 IP + 域名 |

### 1.2 需要开放的端口

去云服务商控制台的安全组里放行：

| 端口 | 用途 |
|---|---|
| 22 | SSH 远程登录 |
| 80 | HTTP 访问 |
| 443 | HTTPS 访问（可选，配置域名证书时需要） |

---

## 二、登录服务器

```bash
ssh root@你的服务器IP
```

如果你的用户名不是 `root`，换成对应的用户名。

---

## 三、安装 Docker

### 3.1 更新系统

```bash
apt update && apt upgrade -y
```

### 3.2 安装 Docker CE（使用阿里云镜像源）

因为服务器在国内，`get.docker.com` 可能连不上，这里用阿里云源：

```bash
# 安装必要工具
apt install -y ca-certificates curl gnupg

# 创建密钥目录
install -m 0755 -d /etc/apt/keyrings

# 添加阿里云 Docker GPG 密钥
curl -fsSL https://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
chmod a+r /etc/apt/keyrings/docker.gpg

# 添加阿里云 Docker 源
cat > /etc/apt/sources.list.d/docker.list << 'EOF'
deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.gpg] https://mirrors.aliyun.com/docker-ce/linux/ubuntu focal stable
EOF

# 安装 Docker
apt update
apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 启动 Docker
systemctl enable docker
systemctl start docker

# 验证
docker --version
docker compose version
```

如果系统不是 Ubuntu 20.04，把上面 `focal` 改成对应的版本代号：
- Ubuntu 22.04 → `jammy`
- Ubuntu 24.04 → `noble`

---

## 四、拉取代码

### 4.1 确保 dev-2.0 分支已推送到 GitHub

在本地项目目录执行：

```bash
cd /home/cjn/Templates/LF/LifeFlow
git push origin dev-2.0
```

提示输入密码时，粘贴 **GitHub Token**（不是 GitHub 登录密码）。

### 4.2 在服务器上克隆代码

```bash
cd /opt
rm -rf LifeFlow   # 如果之前部署过旧版本，先删除
git clone -b dev-2.0 https://github.com/ChenRS-SR/LifeFlow.git
cd LifeFlow/docker
```

---

## 五、配置环境变量

### 5.1 生成 SECRET_KEY

```bash
openssl rand -hex 32
```

复制生成的随机字符串。

### 5.2 创建 .env 文件

```bash
cd /opt/LifeFlow/docker
cp .env.example .env
nano .env
```

按下面格式填写：

```bash
SECRET_KEY=刚才复制的随机字符串
CORS_ORIGINS=http://你的服务器IP,http://localhost,http://localhost:3000,http://127.0.0.1:3000
FRONTEND_URL=http://你的服务器IP
```

示例：

```bash
SECRET_KEY=abcdef1234567890abcdef1234567890abcdef1234567890abcdef12345678
CORS_ORIGINS=http://121.43.239.9,http://localhost,http://localhost:3000,http://127.0.0.1:3000
FRONTEND_URL=http://121.43.239.9
```

保存：`Ctrl + O` → 回车 → `Ctrl + X`。

---

## 六、创建数据目录

```bash
cd /opt/LifeFlow/docker
mkdir -p data certbot/conf certbot/www
```

---

## 七、启动服务

```bash
cd /opt/LifeFlow/docker
docker compose up -d --build
```

第一次启动会拉取镜像并构建前端、后端，耗时约 3-10 分钟，耐心等待。

---

## 八、验证部署

### 8.1 查看容器状态

```bash
cd /opt/LifeFlow/docker
docker compose ps
```

正常应该看到：

```
NAME                STATUS                   PORTS
lifeflow-backend    Up ... (healthy)         8000/tcp
lifeflow-frontend   Up ...                   80/tcp
lifeflow-nginx      Up ...                   0.0.0.0:80->80/tcp, 0.0.0.0:443->443/tcp
lifeflow-certbot    Up ...                   80/tcp, 443/tcp
```

### 8.2 测试健康检查

```bash
curl -s http://localhost/health
```

返回：

```json
{"status":"ok","version":"1.0.0"}
```

### 8.3 测试登录

```bash
curl -s -X POST -d "username=admin&password=admin123" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  http://localhost/api/auth/login
```

应该返回 JWT token。

### 8.4 浏览器访问

打开：

```text
http://你的服务器IP
```

默认账号：

- 用户名：`admin`
- 密码：`admin123`

**登录后第一件事：修改默认密码。**

---

## 九、常见问题

### 9.1 端口 80 被占用

如果启动时报错：

```
failed to bind host port for 0.0.0.0:80: address already in use
```

查一下谁占用了：

```bash
ss -tlnp | grep :80
```

如果是之前手动部署的 Nginx 和 Uvicorn，停掉它们：

```bash
systemctl stop nginx
systemctl disable nginx
pkill -f uvicorn
```

然后重新启动：

```bash
cd /opt/LifeFlow/docker
docker compose up -d
```

### 9.2 Docker 构建时 apt-get 卡住

如果构建后端时 `apt-get update` 卡住超过 10 分钟，是 Debian 官方源太慢。修改 `backend/Dockerfile`，换成国内源：

```dockerfile
FROM python:3.11-slim

WORKDIR /app

RUN sed -i 's/deb.debian.org/mirrors.tuna.tsinghua.edu.cn/g' /etc/apt/sources.list.d/debian.sources \
    && sed -i 's/security.debian.org/mirrors.tuna.tsinghua.edu.cn/g' /etc/apt/sources.list.d/debian.sources \
    && apt-get update \
    && apt-get install -y gcc \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

COPY app/ ./app/
RUN mkdir -p /app/data

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

然后重新构建：

```bash
cd /opt/LifeFlow/docker
docker compose down
docker compose build --no-cache backend
docker compose up -d
```

### 9.3 无法访问网站

1. 检查安全组是否放行 80 端口
2. 检查服务器防火墙：

```bash
ufw status
```

如果没开 80：

```bash
ufw allow 80/tcp
ufw allow 443/tcp
ufw reload
```

3. 看容器日志：

```bash
cd /opt/LifeFlow/docker
docker compose logs -f
```

---

## 十、更新版本

本地修改代码并推送后，在服务器上更新：

```bash
cd /opt/LifeFlow
git pull origin dev-2.0
cd docker
docker compose down
docker compose up -d --build
```

---

## 十一、数据备份

数据库文件在服务器上的路径：

```bash
/opt/LifeFlow/docker/data/lifeflow.db
```

建议定期备份，比如每天一次：

```bash
cp /opt/LifeFlow/docker/data/lifeflow.db /root/lifeflow-backup-$(date +%Y%m%d).db
```

也可以加一条 cron 任务：

```bash
crontab -e
```

添加：

```bash
0 3 * * * cp /opt/LifeFlow/docker/data/lifeflow.db /root/lifeflow-backup-$(date +\%Y\%m\%d).db
```

---

## 十二、配置 HTTPS（可选）

如果有域名，可以参考 `DEPLOY.md` 里的第四章配置 Let's Encrypt 证书。核心步骤：

1. 域名解析到服务器 IP
2. 修改 `docker/nginx.conf` 里的 `server_name` 为你的域名
3. 修改 `docker/.env` 里的 `FRONTEND_URL` 为 `https://你的域名`
4. 申请证书：

```bash
cd /opt/LifeFlow/docker
docker compose stop nginx
docker run -it --rm \
  -v $(pwd)/certbot/conf:/etc/letsencrypt \
  -v $(pwd)/certbot/www:/var/www/certbot \
  -p 80:80 \
  certbot/certbot certonly --standalone \
  -d yourdomain.com -d www.yourdomain.com
```

5. 按 `DEPLOY.md` 修改 `docker/nginx.conf` 启用 HTTPS
6. 重启：

```bash
docker compose up -d
```

---

**部署完成！** 🎉
